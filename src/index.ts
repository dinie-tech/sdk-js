// Curated public barrel — the single entry point consumers import from.
// Module stories re-export `Dinie`, `Webhooks`, error classes, and public types
// (Customer, CustomerCreatedEvent, config/option types) from here.
// Empty stub in the scaffold story so `tsc` does not reference unwritten modules.
export {};
