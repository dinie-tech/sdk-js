/**
 * Branded resource-id aliases + their wire patterns. Each id is a light `string` alias — the
 * prefix lives in the contract, not the type system; the `*_ID_PATTERN` const carries the schema
 * `pattern` verbatim. Generated by sdk-generator from openapi.yaml + sdk-config.yml — do not edit
 * by hand.
 */

/** Identificador único da credencial de API (inclui prefixo de ambiente `live`/`test`) */
export type ApiClientId = string;
/** Identificador único da conta bancária */
export type BankAccountId = string;
/** Identificador único da oferta de crédito */
export type CreditOfferId = string;
/** Identificador único do cliente */
export type CustomerId = string;
/** Identificador único do evento de webhook */
export type EventId = string;
/** Identificador único do empréstimo */
export type LoanId = string;
/** Identificador único da simulação */
export type SimulationId = string;
/** Identificador único da transação */
export type TransactionId = string;
/** Identificador único de uma tentativa de entrega de webhook */
export type WebhookDeliveryId = string;
/** Identificador único do endpoint de webhook */
export type WebhookEndpointId = string;

/** Pattern for {@link ApiClientId}. */
export const API_CLIENT_ID_PATTERN = /^dinie_ci_(live|test)_[A-Za-z0-9]+$/;
/** Pattern for {@link BankAccountId}. */
export const BANK_ACCOUNT_ID_PATTERN = /^ba_[0-9a-f]{32}$/;
/** Pattern for {@link CreditOfferId}. */
export const CREDIT_OFFER_ID_PATTERN = /^co_[0-9a-f]{32}$/;
/** Pattern for {@link CustomerId}. */
export const CUSTOMER_ID_PATTERN = /^cust_[0-9a-f]{32}$/;
/** Pattern for {@link EventId}. */
export const EVENT_ID_PATTERN = /^evt_[0-9a-f]{32}$/;
/** Pattern for {@link LoanId}. */
export const LOAN_ID_PATTERN = /^ln_[0-9a-f]{32}$/;
/** Pattern for {@link SimulationId}. */
export const SIMULATION_ID_PATTERN = /^sim_[0-9a-f]{32}$/;
/** Pattern for {@link TransactionId}. */
export const TRANSACTION_ID_PATTERN = /^tx_[0-9a-f]{32}$/;
/** Pattern for {@link WebhookDeliveryId}. */
export const WEBHOOK_DELIVERY_ID_PATTERN = /^dlv_[0-9a-f]{32}$/;
/** Pattern for {@link WebhookEndpointId}. */
export const WEBHOOK_ENDPOINT_ID_PATTERN = /^we_[0-9a-f]{32}$/;
