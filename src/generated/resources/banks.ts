/**
 * Generated SDK resource: a class composing methods on one injected HttpClient — `Promise<T>`
 * (non-list), `Promise<T[]>` (flat list), `PagePromise<T>` (cursor list, auto-paginated). Casing
 * is delegated to the per-type (de)serializers. Generated by sdk-generator from openapi.yaml +
 * sdk-config.yml — do not edit by hand.
 */
import type { HttpClient, RequestOptions } from '../../runtime/http.js';
import { type Bank, type BankWire, deserializeBank } from '../types/bank.js';

/** Path of the banks collection. */
const BANKS_PATH = '/banks';

/** Wire shape of the `/banks` response: a flat `{ data }` envelope. */
interface BankListWire {
  data: BankWire[];
}

/**
 * Operations on the banks resource.
 */
export class Banks {
  readonly #http: HttpClient;

  constructor(http: HttpClient) {
    this.#http = http;
  }

  /**
   * List banks
   *
   * List banks available for customer bank account submission.
   *
   * @param options Request options.
   */
  async list(options?: RequestOptions): Promise<Bank[]> {
    const wire = await this.#http.request<BankListWire>({
      method: 'GET',
      path: BANKS_PATH,
      idempotent: false,
      ...(options !== undefined ? { options } : {}),
    });
    return wire.data.map(deserializeBank);
  }
}
