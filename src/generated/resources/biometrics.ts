/**
 * Generated SDK resource: a class composing methods on one injected HttpClient — `Promise<T>`
 * (non-list), `Promise<T[]>` (flat list), `PagePromise<T>` (cursor list, auto-paginated). Casing
 * is delegated to the per-type (de)serializers. Generated by sdk-generator from openapi.yaml +
 * sdk-config.yml — do not edit by hand.
 */
import type { HttpClient, RequestOptions } from '../../runtime/http.js';
import {
  type BiometricsSessionExchangeResponse,
  type BiometricsSessionExchangeResponseWire,
  deserializeBiometricsSessionExchangeResponse,
} from '../types/biometrics-session-exchange-response.js';

/** Path of the biometrics collection. */
const BIOMETRICS_PATH = '/biometrics/session-exchange';

export class Biometrics {
  readonly #http: HttpClient;

  constructor(http: HttpClient) {
    this.#http = http;
  }

  async sessionExchange(
    params?: Record<string, unknown>,
    options?: RequestOptions,
  ): Promise<BiometricsSessionExchangeResponse> {
    const wire = await this.#http.request<BiometricsSessionExchangeResponseWire>({
      method: 'POST',
      path: BIOMETRICS_PATH,
      body: params ?? {},
      idempotent: true,
      ...(options !== undefined ? { options } : {}),
    });
    return deserializeBiometricsSessionExchangeResponse(wire);
  }
}
